# 海量高并发

## **容量预估**

> 用户路径梳理，接口裁剪&QPS预估，关注木桶效应（前端/接入/逻辑/存储/依赖第三方）

💡 一句话总结

**后台容量预估：梳理核心用户路径识别关键接口，基于业务数据预估QPS，全链路评估前端/接入/逻辑/存储/依赖五层容量，重点关注木桶效应的最短板，通过压测验证和监控预警保障系统稳定。**



### 📊 用户路径梳理

#### **1. 核心用户旅程地图**

<img src="系统架构篇（高级）.assets/image-20260304193816455.png" alt="image-20260304193816455" style="zoom:33%;" />

#### **2. 接口依赖关系**

```yaml
# 关键接口依赖分析
user_journey_interfaces:
  注册流程:
    - 验证码获取: 依赖短信服务
    - 注册提交: 依赖用户服务、风控服务
    - 初始化数据: 依赖配置服务、推荐服务
    
  下单支付:
    - 购物车查询: 依赖购物车服务
    - 库存检查: 依赖库存服务
    - 优惠计算: 依赖营销服务
    - 支付创建: 依赖支付渠道
    - 订单创建: 依赖订单服务
    - 库存扣减: 依赖库存服务
```

### ⚡ 接口裁剪与QPS预估

#### **1. 接口重要性分级**

```yaml
# 接口SLA分级（Service Level Agreement（承诺））
interface_sla_levels:
  P0_核心交易接口:  # 必须保障
    - 下单接口
    - 支付接口
    - 库存查询
    - SLA: 99.99%，响应时间 < 100ms
    
  P1_核心浏览接口:  # 高优先保障
    - 商品详情
    - 用户登录
    - 购物车操作
    - SLA: 99.9%，响应时间 < 200ms
    
  P2_辅助功能接口:  # 可降级
    - 商品评论
    - 用户画像
    - 推荐接口
    - SLA: 99%，响应时间 < 500ms
    
  P3_后台管理接口:  # 可限制
    - 数据报表
    - 配置管理
    - 运营功能
    - SLA: 95%，响应时间 < 1s
```

#### **2. QPS预估公式**

```python
# 基于业务数据的QPS预估
class QPSEstimator:
    
    def estimate_peak_qps(self, business_data):
        """
        计算峰值QPS
        公式: 峰值QPS = 预估用户数 × 转化率 × 接口调用频率
        """
        # 基础数据
        estimated_users = 1000000  # 预估用户数
        daily_active_rate = 0.2   # 日活率
        peak_hour_ratio = 0.15    # 峰值小时占比
        conversion_rate = 0.1     # 转化率
        
        # 计算
        daily_active_users = estimated_users * daily_active_rate
        peak_hour_users = daily_active_users * peak_hour_ratio
        peak_concurrent_users = peak_hour_users * 0.1  # 10%并发
        
        # 接口调用频率
        api_call_per_user_per_hour = {
            "homepage": 5,      # 首页: 5次/用户/小时
            "product_detail": 3, # 商品详情: 3次
            "add_to_cart": 0.5,  # 加购: 0.5次
            "place_order": 0.1,  # 下单: 0.1次
        }
        
        # 计算各接口峰值QPS
        for api, call_rate in api_call_per_user_per_hour.items():
            qps = peak_concurrent_users * call_rate / 3600
            print(f"{api} 峰值QPS: {qps:.2f}")
        
        return peak_concurrent_users
```

### 🎯 全链路容量评估（木桶效应）

#### **1. 五层容量瓶颈分析**

<img src="系统架构篇（高级）.assets/image-20260304194218313.png" alt="image-20260304194218313" style="zoom:50%;" />

#### **2. 各层容量评估**

**前端/客户端层**

```yaml
frontend_capacity:
  瓶颈点:
    - 浏览器并发连接数: 6-8个/域名
    - 移动端网络连接: 弱网环境
    - 资源加载: 图片/JS/CSS大小
    
  优化措施:
    - CDN加速静态资源
    - 资源合并与压缩
    - 懒加载策略
    - 连接复用
```

**接入层**

```yaml
gateway_capacity:
  关键组件:
    - Nginx/OpenResty
    - API网关
    - 负载均衡器
    
  容量指标:
    - 连接数: 受限于文件描述符
    - 并发请求: 受限于worker进程
    - 带宽: 网络吞吐量
    
  配置示例:
    nginx:
      worker_processes: auto
      worker_connections: 10240
      keepalive_timeout: 65
      client_max_body_size: 10m
```

**逻辑层（应用服务）**

```yaml
application_capacity:
  评估维度:
    - CPU: 核心业务逻辑计算
    - 内存: 缓存、对象存储
    - 线程池: 并发处理能力
    - 连接池: 数据库/Redis连接
    
  计算公式:
    # 单实例QPS
    单实例QPS = 可用线程数 × 单线程处理能力
    
    # 所需实例数
    所需实例数 = 预估峰值QPS / 单实例QPS × 安全系数(1.5-2)
    
  示例:
    预估峰值QPS: 1000
    单实例QPS: 200
    安全系数: 1.5
    所需实例数 = 1000 / 200 × 1.5 = 7.5 ≈ 8个
```

**存储层**

```yaml
storage_capacity:
  # 数据库评估
  database:
    读写比例: 读写比 8:2
    连接数: 最大连接数配置
    QPS: 受限于磁盘IOPS
    容量: 数据增长预估
    
  # Redis评估
  cache:
    内存容量: 热点数据大小 × 副本数
    QPS: 10万+/秒（单实例）
    连接数: 最大客户端连接
    持久化: AOF/RDB影响
    
  # 消息队列
  message_queue:
    吞吐量: 消息生产/消费速率
    积压: 消费延迟监控
    分区: 分区数影响并发
```

**依赖服务**

```yaml
dependency_capacity:
  评估要点:
    - 第三方API限流
    - 服务间调用超时
    - 熔断降级策略
    - 备用方案准备
  
  风险矩阵:
    - 支付渠道: 必须多路备用
    - 短信服务: 可降级为异步
    - 地图服务: 可缓存结果
    - 风控服务: 可本地简化规则
```

### 📈 容量压测与验证

#### **1. 压测策略**

```yaml
pressure_test_strategy:
  # 压测类型
  types:
    - 基准测试: 单接口基准性能
    - 负载测试: 逐步增加负载
    - 压力测试: 达到并超过峰值
    - 稳定性测试: 长时间运行
    - 破坏性测试: 模拟异常场景
  
  # 压测目标
  targets:
    - 找到系统瓶颈
    - 验证容量预估
    - 测试弹性伸缩
    - 验证降级方案
```

#### **2. 压测场景设计**

```python
# 使用JMeter/Locust设计压测场景
class PressureTestScenarios:
    
    def create_scenarios(self):
        scenarios = {
            "双11大促": {
                "用户数": 10000,
                "持续时间": 7200,  # 2小时
                "业务比例": {
                    "浏览商品": 40,    # 40%
                    "加入购物车": 30,  # 30%
                    "下单支付": 20,    # 20%
                    "其他操作": 10,    # 10%
                },
                "峰值倍数": 3,  # 平时流量的3倍
            },
            
            "秒杀活动": {
                "用户数": 5000,
                "持续时间": 300,   # 5分钟
                "业务比例": {
                    "查询库存": 50,
                    "提交订单": 50,
                },
                "特性": "瞬间高并发，需要排队限流"
            }
        }
        return scenarios
```

### 🔧 容量规划工具

#### **1. 容量计算器**

```python
# 容量规划计算器
class CapacityCalculator:
    
    def calculate_required_instances(self, peak_qps, instance_qps, 
                                     redundancy_factor=1.5):
        """计算所需实例数"""
        return math.ceil(peak_qps / instance_qps * redundancy_factor)
    
    def estimate_storage_growth(self, current_size, growth_rate, months):
        """预估存储增长"""
        return current_size * ((1 + growth_rate) ** months)
    
    def check_bottleneck(self, system_metrics):
        """识别瓶颈点"""
        bottlenecks = []
        
        # CPU检查
        if system_metrics['cpu_usage'] > 70:
            bottlenecks.append("CPU使用率过高")
            
        # 内存检查
        if system_metrics['memory_usage'] > 80:
            bottlenecks.append("内存使用率过高")
            
        # 连接数检查
        if system_metrics['connection_usage'] > 80:
            bottlenecks.append("连接数接近上限")
            
        return bottlenecks
```

#### **2. 监控预警配置**

```yaml
# 容量监控预警规则
capacity_alerts:
  cpu_alert:
    condition: "cpu_usage > 70% 持续5分钟"
    level: "warning"
    action: "检查扩容或优化代码"
    
  memory_alert:
    condition: "memory_usage > 80% 持续5分钟"
    level: "critical"
    action: "立即扩容或优化内存使用"
    
  qps_alert:
    condition: "current_qps > 80%_of_peak_qps"
    level: "warning"
    action: "准备弹性扩容"
    
  storage_alert:
    condition: "storage_usage > 85%"
    level: "warning"
    action: "清理数据或扩容存储"
```

### 🎯 应急预案

#### **1. 容量不足应对**

```yaml
emergency_plans:
  # 扩容策略
  scale_out_strategy:
    - 一级扩容: 自动弹性伸缩
    - 二级扩容: 手动增加实例
    - 三级扩容: 启用备用机房
    
  # 降级策略
  degrade_strategy:
    - 非核心功能降级
    - 缓存过期时间缩短
    - 同步改异步处理
    - 返回简化数据
    
  # 限流策略
  rate_limit_strategy:
    - 用户级限流
    - IP级限流
    - 接口级限流
    - 排队机制
```

#### **2. 资源预留策略**

```yaml
resource_reservation:
  # 计算资源
  compute:
    - 平时: 70% 使用率
    - 预留: 30% 缓冲容量
    - 紧急: 可超卖20%
  
  # 网络带宽
  bandwidth:
    - 平时: 60% 使用率
    - 预留: 40% 突发容量
  
  # 数据库连接
  database:
    - 平时: 80% 连接数
    - 预留: 20% 紧急连接
```



## **全链路压测**

> 请求标注&环境隔离，流量复制 TcpCopy/GoReplay，用例校准，瓶颈定位，环境清理&用例回归

💡 一句话总结

**全链路压测：通过流量标注实现压测流量识别，利用TCPCopy/GoReplay复制真实流量，在隔离环境中执行场景化压测，精准定位系统瓶颈，压测后彻底清理环境并回归验证，确保系统稳定性和性能达标。**



### 🎯 全链路压测核心流程

<img src="系统架构篇（高级）.assets/image-20260304194741096.png" alt="image-20260304194741096" style="zoom:50%;" />



### 🔧 关键技术实现

#### **1. 请求标注 - 识别压测流量**

```java
// 在HTTP Header中标记压测流量
public class TestFlagInterceptor implements HandlerInterceptor {
    
    private static final String TEST_FLAG_HEADER = "X-Pressure-Test";
    private static final String TEST_FLAG_VALUE = "true";
    private static final String TEST_USER_ID_PREFIX = "test_";
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) {
        // 检查是否为压测流量
        String testFlag = request.getHeader(TEST_FLAG_HEADER);
        boolean isPressureTest = TEST_FLAG_VALUE.equals(testFlag);
        
        if (isPressureTest) {
            // 标记当前线程上下文
            MDC.put("is_pressure_test", "true");
            
            // 修改用户ID，避免污染生产数据
            String userId = request.getHeader("X-User-ID");
            if (userId != null && !userId.startsWith(TEST_USER_ID_PREFIX)) {
                request.setAttribute("original_user_id", userId);
                request.setHeader("X-User-ID", TEST_USER_ID_PREFIX + userId);
            }
            
            // 使用压测配置
            ConfigManager.useTestConfig();
        }
        
        return true;
    }
}
```

#### **2. 数据隔离策略**

```yaml
# 数据库层面隔离方案
database_isolation:
  # 方案1: 影子表
  shadow_tables:
    - 原表: users
    - 影子表: users_shadow
    - 规则: 压测流量写影子表
    
  # 方案2: 数据库中间件
  middleware_routing:
    - 正常SQL: 路由到主库
    - 压测SQL: 路由到影子库
    - 识别方式: 通过特殊注解或连接属性
    
  # 方案3: 字段标记
  column_marking:
    - 增加字段: is_test_data BOOLEAN
    - 查询过滤: WHERE is_test_data = false
    - 压测插入: SET is_test_data = true
    
# 缓存隔离
cache_isolation:
  key_prefix: "test:"
  ttl_shorter: 300  # 压测缓存TTL缩短
  separate_cluster: true  # 使用独立Redis集群
```

### 📡 流量复制方案

#### **1. TCPCopy - 底层流量复制**

```bash
# TCPCopy架构：在线实时复制
# 生产服务器（源）
./tcpcopy -x 80-192.168.1.100:80 -s 192.168.1.101 -c 192.168.1.x -n 2

# 辅助服务器（intercept）
./intercept -F "tcp and src port 80" -i eth0

# 参数说明：
# -x: 源端口-目标IP:目标端口
# -s: 辅助服务器IP
# -c: 客户端IP网段
# -n: 复制倍数

# 特点：
# ✅ 复制真实TCP流量
# ✅ 支持倍数放大
# ✅ 保持连接状态
# ❌ 需要内核模块
# ❌ 配置较复杂
```

#### **2. GoReplay - 应用层流量复制**

```bash
# GoReplay使用示例
# 1. 录制生产流量
sudo ./gor --input-raw :8080 --output-file requests.gor

# 2. 回放到测试环境
sudo ./gor --input-file requests.gor --output-http "http://test-env:8080"

# 3. 实时复制（带过滤）
sudo ./gor --input-raw :8080 \
  --output-http "http://test-env:8080" \
  --http-allow-method GET \
  --http-allow-method POST \
  --http-disallow-url /admin \
  --output-http-stats

# 4. 流量放大
sudo ./gor --input-file "requests.gor" \
  --output-http "http://test-env:8080" \
  --output-http-workers 10 \
  --output-http-timeout 5s \
  --input-file-loop \
  --speed 2  # 2倍速播放

# 特点：
# ✅ 无需内核模块
# ✅ 支持HTTP过滤
# ✅ 可离线回放
# ❌ 不复制TCP层流量
```

#### **3. 流量染色与过滤**

```yaml
# 流量过滤规则配置
traffic_filter_rules:
  # 按URL过滤
  url_patterns:
    include:
      - "/api/v1/orders/**"
      - "/api/v1/payments/**"
    exclude:
      - "/api/v1/admin/**"
      - "/api/v1/reports/**"
  
  # 按请求头过滤
  header_filters:
    include:
      - "User-Agent: Mobile*"
    exclude:
      - "X-Forwarded-For: 10.*"
  
  # 按业务参数过滤
  param_filters:
    include:
      - "amount > 1000"  # 大额订单
    exclude:
      - "test_user = true"  # 已经是测试用户
  
  # 采样率控制
  sampling_rate: 0.1  # 10%流量采样
```

### 📊 压测用例校准

#### **1. 基准性能测试**

```yaml
# 建立性能基线
performance_baseline:
  single_instance:
    cpu: "4核"
    memory: "8GB"
    expected_qps: 1000
    expected_p99_latency: 100ms
    
  stress_points:
    - cpu_bound_operations:
        - 加密解密
        - 图片处理
        - 复杂计算
        
    - io_bound_operations:
        - 数据库查询
        - 文件读写
        - 网络请求
        
    - memory_bound_operations:
        - 大对象缓存
        - 批量数据处理
```

#### **2. 场景化用例设计**

```yaml
# 电商大促场景
pressure_test_scenarios:
  double_11:
    concurrent_users: 10000
    duration_minutes: 120
    business_mix:
      browse_products: 40%
      search_products: 20%
      add_to_cart: 15%
      checkout: 15%
      payment: 10%
    
    peak_traffic_pattern:
      - 00:00-00:05: 500%  # 零点峰值
      - 10:00-11:00: 200%  # 早高峰
      - 20:00-21:00: 300%  # 晚高峰
    
    special_cases:
      - flash_sale: 1000并发抢100商品
      - coupon_use: 80%用户使用优惠券
      - stock_update: 实时库存更新
```

### 🔍 瓶颈定位分析

#### **1. 性能瓶颈检查清单**

```bash
# 全链路性能检查点
# 1. 前端/客户端
检查: 资源加载、JS执行、网络延迟
工具: Chrome DevTools, Lighthouse

# 2. 网络层
检查: 带宽、延迟、丢包、DNS
工具: ping, traceroute, mtr, iftop

# 3. 负载均衡
检查: 会话保持、健康检查、连接复用
工具: Nginx日志, HAProxy Stats

# 4. 应用服务
检查: CPU、内存、线程、GC
工具: JProfiler, Arthas, VisualVM

# 5. 中间件
检查: 连接池、队列深度、缓存命中
工具: Redis CLI, RabbitMQ管理台

# 6. 数据库
检查: 慢查询、锁等待、IOPS、连接数
工具: EXPLAIN, pt-query-digest, SHOW PROCESSLIST

# 7. 外部依赖
检查: 第三方API响应、熔断状态
工具: 分布式追踪, 外部监控
```

#### **2. 瓶颈定位工具链**

```yaml
# 全链路监控工具集成
monitoring_stack:
  # 基础设施监控
  infrastructure:
    - node_exporter: 主机指标
    - cadvisor: 容器指标
    - nvidia_gpu_exporter: GPU监控
  
  # 应用性能监控
  application:
    - jaeger: 分布式追踪
    - prometheus: 指标收集
    - grafana: 数据可视化
    - skywalking: APM监控
  
  # 业务监控
  business:
    - elasticsearch: 日志分析
    - kibana: 日志可视化
    - sentinel: 流量控制
    - arthas: Java诊断
  
  # 压测工具
  pressure_test:
    - jmeter: 负载测试
    - wrk: HTTP压测
    - vegeta: HTTP压测
    - locust: 分布式压测
```

#### **3. 瓶颈分析报告**

```python
# 自动化瓶颈分析报告
class BottleneckAnalyzer:
    
    def analyze_performance_data(self, metrics_data):
        """分析性能数据，识别瓶颈"""
        
        bottlenecks = []
        
        # CPU分析
        if metrics_data['cpu_usage'] > 80:
            bottlenecks.append({
                'type': 'CPU瓶颈',
                'severity': 'high',
                'description': f"CPU使用率 {metrics_data['cpu_usage']}% 过高",
                'suggestion': '优化计算密集型代码，增加实例数'
            })
        
        # 内存分析
        if metrics_data['gc_time'] > 100:  # GC时间过长
            bottlenecks.append({
                'type': '内存GC瓶颈',
                'severity': 'high',
                'description': f"Full GC时间 {metrics_data['gc_time']}ms",
                'suggestion': '优化内存使用，调整JVM参数'
            })
        
        # 数据库分析
        if metrics_data['slow_queries'] > 10:
            bottlenecks.append({
                'type': '数据库慢查询',
                'severity': 'medium',
                'description': f"慢查询数量 {metrics_data['slow_queries']}",
                'suggestion': '优化SQL，添加索引，分库分表'
            })
        
        return bottlenecks
```

### 🧹 环境清理与用例回归

#### **1. 压测数据清理**

```sql
-- 自动化清理脚本示例
-- 1. 清理压测用户数据
DELETE FROM users 
WHERE created_at >= '2024-01-15 00:00:00'
  AND username LIKE 'test_%';

-- 2. 清理压测订单
DELETE FROM orders 
WHERE is_test_data = true
  AND created_at >= DATE_SUB(NOW(), INTERVAL 1 DAY);

-- 3. 清理缓存
DEL redis-cli KEYS "test:*" | xargs redis-cli DEL

-- 4. 清理消息队列
# 消费掉所有测试队列消息
# 或者直接删除测试队列

-- 5. 清理文件存储
find /data/uploads -name "*test*" -type f -delete
```

#### **2. 自动化清理流水线**

```yaml
# GitLab CI/CD清理流水线
cleanup_pipeline:
  stages:
    - cleanup
    
  cleanup_test_data:
    stage: cleanup
    script:
      - |
        # 1. 停止压测服务
        kubectl delete -f pressure-test-deployment.yaml
        
        # 2. 清理数据库
        ./scripts/cleanup_test_data.sh
        
        # 3. 清理缓存
        redis-cli -h $REDIS_HOST --scan --pattern "test:*" | xargs redis-cli del
        
        # 4. 清理日志
        find /var/log/pressure-test -type f -mtime +7 -delete
        
        # 5. 清理临时文件
        rm -rf /tmp/pressure_test_*
        
        # 6. 发送清理报告
        ./scripts/send_cleanup_report.sh
        
    only:
      - pressure_test
    when: always
```

#### **3. 回归测试验证**

```yaml
# 压测后回归测试
regression_tests:
  # 基础功能验证
  basic_functionality:
    - 用户登录注册
    - 核心下单流程
    - 支付流程
    - 商品浏览
    
  # 性能回归验证
  performance_regression:
    - 单接口响应时间对比
    - 并发处理能力验证
    - 资源使用率对比
    
  # 数据一致性验证
  data_consistency:
    - 订单金额一致性
    - 库存准确性
    - 用户余额正确性
    
  # 监控告警验证
  monitoring_verification:
    - 告警规则触发测试
    - 日志采集完整性
    - 监控数据准确性
```

#### **4. 压测报告生成**

```python
# 自动化压测报告生成
class PressureTestReporter:
    
    def generate_report(self, test_id, results):
        """生成压测报告"""
        
        report = {
            "summary": {
                "test_id": test_id,
                "duration": results['duration'],
                "peak_qps": results['peak_qps'],
                "success_rate": results['success_rate'],
                "p99_latency": results['p99_latency']
            },
            "bottlenecks": self.analyze_bottlenecks(results),
            "recommendations": self.generate_recommendations(results),
            "resource_usage": results['resource_metrics'],
            "comparison": self.compare_with_baseline(results),
            "next_steps": self.suggest_next_steps(results)
        }
        
        # 生成可视化图表
        self.generate_charts(report)
        
        # 发送到指定渠道
        self.send_report(report, ['slack', 'email', 'confluence'])
        
        return report
```

### 🎯 最佳实践总结

#### **1. 压测安全准则**

```yaml
pressure_test_safety_rules:
  # 环境隔离
  - 必须使用独立测试环境
  - 生产数据必须脱敏
  - 测试流量必须明确标记
  
  # 数据保护
  - 禁止修改生产数据
  - 测试数据定时清理
  - 敏感信息必须加密
  
  # 监控告警
  - 压测期间加强监控
  - 设置熔断机制
  - 准备一键停止方案
  
  # 团队协作
  - 提前通知相关团队
  - 制定应急预案
  - 明确回滚步骤
```

#### **2. 压测执行检查清单**

```markdown
## 压测前检查
- [ ] 环境已隔离，与生产完全独立
- [ ] 压测流量已正确标记
- [ ] 监控告警已配置
- [ ] 应急方案已准备
- [ ] 团队已通知到位
- [ ] 数据库备份已完成

## 压测中监控
- [ ] 核心指标实时监控
- [ ] 错误率在可接受范围
- [ ] 资源使用率正常
- [ ] 依赖服务状态正常
- [ ] 业务数据一致性验证

## 压测后清理
- [ ] 压测服务已停止
- [ ] 测试数据已清理
- [ ] 环境已恢复
- [ ] 报告已生成
- [ ] 问题已记录跟踪
```









- **横向扩容 Scale-out**：逻辑层做分布式微服务拆分，存储层引入分布式数据库提升伸缩性
- **访问限流**：业务侧提前预约/设验证码/限制重试，系统侧基于API网关做限流熔断/过载保护
- **性能分析**：链路追踪 Tracing，应用分析 pprof/torch，性能4大金刚（CPU/内存/磁盘/网络）
- **服务性能优化实践**：关注锁粒度/异步处理/日志缓冲/队列丢包/内核参数net.core.somaxconn...
- **数据库优化**：分片sharding（TiDB）、业务分离、读写分离、链接池&链接代理、慢SQL优化、参数调优...
- **缓存Cache**：本地缓存/分布式缓存区别，读写策略，关注缓存穿透/击穿/雪崩问题，关注BigKey/HotKey
- **消息队列MQ**：流量削峰/异步处理/应用耦合、消息可靠性/顺序性/唯一性（重试/幂等），关注消息延迟堆积监控
- **静态资源**：CDN加速，预加载策略 Preload，图片优化（格式webp/合并sprite/压缩/懒加载）



# end

# 系统高可用

- **影响因素**：机房故障、网络抖动、计算/存储资源不够、代码bug、依赖系统问题、城市级不可抗地震水灾...
- **衡量指标**：可用性百分比（x个9），服务等级协议 SLA，MTBF&MTTR
- **分布式理论**：CAP/BASE理论，一致性协议Paxos/Raft/ZAB，选举策略和脑裂问题解决方案，对比etcd（Raft协议/简洁易维护/基于go云原生）/zookeeper（ZAB类Paxos协议/复杂难懂依赖多）
- **故障模式与影响分析 FMEA**：挖掘系统可用性隐患，业务功能/故障模式/影响范围/风险程度/解决措施/规划代办...
- **冗余架构**：同城双活（基础要求），两地三中心（评估ROI/功能分级/跨IDC数据同步方案）
- **业务隔离**：业务按重要性分级，基于业务/地域/编号做分SET部署和灰度发布，关注SET预留容量
- **快速故障转移**：客户端做失败重试，API网关做故障判定和转移，引入HA/健康心跳/长短连拨测策略
- **核心路径柔性降级**：偏产品策略，接口失败放过/补默认数据/用缓存数据/直播降码率...
- **运营保障**：例行全链路压测，混沌工程&容灾演练，特性开关做快速恢复，活动报备，值班巡检和SOP...

# 可扩展性

- **设计原则**：合适/简单/演进，模块高内聚低耦合，适当重构
- **分层架构**：用户层/接入层/逻辑层/基础层/存储层，明确各层职责，降低系统耦合度
- **微服务模块化**：基于DDD做服务模块拆分，变化/稳定分离，接口隔离，没跨模块数据层调用





# 系统安全

- **理论基础**：安全原则CIA 机密性/完整性/可用性，黄金法则 认证/授权/审计
- **密码学**：熟悉3种经典加密算法及场景，对称加密AES/非对称加密RSA/散列算法SHA256加盐（不可逆）
- **Web安全**：熟悉4类常见攻击 XSS/SQL/CSRF/SSRF，攻击原理/危害案例/防御方案
- **数据安全**：用户隐私类等敏感数据（手机号/身份证），全流程加密传输（https）和加密存储（AES）
- **云组件安全**：云账号拆细，关注弱密码和最小授权原则，定期云顾问安全扫描...
- **编码安全**：集成安全扫描门禁，关注明文秘钥/越权漏洞/高危组件/参数校验/日志审计...
- **黑灰产对抗**：提升黑产成本，业务侧条件限制/用户限频/链路鉴权/业务风控/机器学习，防误伤弹验证码...
- **业务安全**：清晰业务安全隐患点，关注账号安全/内容安全/支付安全/活动薅羊毛/防盗版/防欺诈/短信炸弹...







# 典型业务系统

- **接入系统**：用户长链管理 WebSockst，心跳保活机制 KeepAlive，了解运营商网络/跑马竞速/域名劫持/HTTPDNS等全网调度策略...
- **账号系统**：账号注册/登陆/验票/注销流程方案设计，OAuth2.0认证流程，账号安全策略，RBAC访问控制...
- **支付系统**：分布式事务解决方案，基于XA协议的2PC/补偿事务TCC/基于MQ的最终一致性（幂等重试/异步对账）/本地消息表（最大努力通知），行业解决方案Seate（AT/TCC/Saga/XA模式）...
- **消息IM系统**：了解单聊/群聊/在线状态/关系链/离线消息等IM方案设计，保证消息实时性/可靠性/时序性的优化策略
- **直播系统**：编解码技术（H.264/AVC），流媒体传输协议（WebRTC/RTMP/HLS），直播质量体系（QoE/QoS），直播指标优化（首帧/播放成功率/断开率/卡顿率等）...
- **资料系统**：多级缓存组件性能/持久化对比（DB/Redis/ES...），数据同步机制 DTS，数据一致性校验/修复...
- **活动运营**：搭建低代码业务引擎提效（营销/积分/任务/抽奖/发货...），灰黑产对抗和防薅羊毛
- **其他系统**：如推荐系统、广告系统、开放平台、数据仓库...



# 项目实战

- **项目介绍**：介绍下这个项目？
- **承担角色**：你在项目中担任什么角色？团队怎么分工协作？
- **业务数据**：关注哪些业务核心数据？具体数据是多少？
- **竞品分析**：当时项目在行业内竞品有哪些？你们有什么业务/技术竞争力？
- **技术难点**：这个项目有什么技术难点？你是怎么解决的？
- **选型对比**：项目每个技术难点的行业方案是怎么样的？有没有进行选型对比？
- **架构设计**：项目的系统架构和技术栈是怎么样的？每个点是否合理？
- **系统瓶颈**：当前系统的瓶颈在哪里？**用户量/数据量扩大100倍能否支撑住？**
- **海量高并发**：该项目你是怎么支持海量高并发的？
- **系统高可用**：该项目你是怎么做系统高可用的？
- **可扩展性**：该项目你是怎么提高系统可扩展性的？
- **系统安全**：整个项目的业务和系统安全你关注哪些方面？具体做了哪些保障措施？
- **运营成本**：项目运营成本由哪些构成？有哪些成本优化方案？
- **系统部署**：项目当时接入/逻辑/存储是怎么部署的？哪些城市？多少核心？是否合理？
- **依赖组件**：依赖哪些中间件？版本和配置是什么？对应单价是多少？
- **技术指标**：关注哪些系统技术核心指标？值是多少？有什么优化方案？
- **监控体系**：项目的监控体系是怎么搭建的？**发现问题到问题恢复**一般要多久？
- **故障机制**：发生过最大的故障是什么？怎么解决的？有什么经验总结？
- **用户反馈**：用户反馈流程是怎样的？日常反馈量和主要问题？客诉处理时间是多久？
- **用户体验**：团队关注产品的用户体验吗？日常是怎么做的？
- **项目总结**：再回头看，项目有哪些地方做得好的？哪些地方做得不好的？
- **未来规划**：后面项目的主要规划是什么？





# end